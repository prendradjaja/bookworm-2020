Put whatever scripts here, whether they'll be used frequently (probably add
these to package.json too) or they're just throwaway code (if it's likely
that pieces of them will be reused in the future)!
